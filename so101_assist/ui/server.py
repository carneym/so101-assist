"""FastAPI app: serves static/index.html and a WebSocket that streams
- annotated overhead + wrist JPEG frames (overlay.py)
- system state transitions (for on-screen + spoken announcements)
- planned path preview for CONFIRM state
Accepts click-to-select target (Phase 2, pre-voice) and settings.
"""
